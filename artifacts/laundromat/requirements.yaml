packages:
  "framer-motion": "For page transitions and smooth UI animations"
  "date-fns": "For formatting dates easily"
  "clsx": "Utility for constructing className strings conditionally"
  "tailwind-merge": "Utility function to efficiently merge Tailwind CSS classes"
  "lucide-react": "Beautiful, consistent icons"

images:
  - path: "artifacts/laundromat/public/images/laundry-hero.png"
    description: "Clean, modern, light and airy laundromat interior with subtle blue accents, minimal style, soft lighting, professional aesthetic"
    aspect_ratio: "16:9"

notes:
  - Assumes standard REST API provided by the backend at /api
  - Uses @workspace/api-client-react for TanStack Query hooks generated by Orval
